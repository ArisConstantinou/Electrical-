Original prompt: Build a separate small Rapier bulldozer destruction prototype first, ugly but honest, to test whether real physics destruction is worth expanding.

Latest note 2026-06-17: Added the first pass of an advanced demolition replay and inspection system. Recording now starts from the bulldozer wall-contact damage probe, captures sampled global frames plus per-wall tracks, stores wall damage/crack/detach/destroy/roof/full-demolition event markers, and uses hidden cloned replay proxies so removed/disposed wall blocks can still be reconstructed during playback without re-running Rapier physics. The H tuning panel now has a `Demolition Replay` folder with Replay/Play, Pause, Stop, Back, Forward, timeline scrubber, live `H Value` speed slider, wall selector, focus/isolate wall toggles, ghost original structure toggle, reverse reconstruction toggle, camera mode selector, and stats/marker readouts. Replay mode hides live physics sources, drives proxy transforms from recorded timestamped samples with interpolation, and supports follow-wall/top-down/cinematic camera modes. Added `prototype_demolition_replay_for_test()` and `?replayTest=<frames>`, plus replay debug state in `render_game_to_text`. Verification: `npm.cmd run build` passes; required develop-web-game smoke `?v=demolition-replay-smoke-2&replayTest=260` completed with no browser error file. Captured replay state reports `complete:true`, `active:true`, `frames:30`, `events:36`, `proxyObjects:266`, `proxiesVisible:40`, `selectedWall:"front"`, `duration:2.83`, and front wall track `samples:30`. Screenshot `output/demolition-replay-smoke/shot-0.png` shows the H-panel replay controls, front wall selected, focus-wall enabled, ghost original enabled, follow-wall camera, selected-wall highlight box, and replayed debris/structure proxies visible.

Latest note 2026-06-17: Reduced the first-contact slowdown and individual block rotation/fan-out when a falling wall panel lands on the bulldozer. Root cause for the odd second-image look was large per-block spin in `releaseWallFaceToFallFlat()` (`4.8+` angular velocity) plus bulldozer separation zeroing/pushing chunks independently. Structural collapse debris now gets face metadata, higher angular damping, lower linear damping, much lower release spin (`0.72` base), and `driveStructuralWallDebrisTowardFlatFall()` enters a panel-coherence phase for fresh or near-bulldozer structural debris with low shared spin, stronger angular damping, and less individual angular carry-over. The bulldozer separator now damps structural debris rotations instead of snapping X/Z to zero, and structural wall chunks receive a smaller sideways push (`0.42` instead of `1.4`) during dozer separation. Verification: `npm.cmd run build` passes; required develop-web-game smoke `?v=coherent-panel-dozer-2&bulldozerBlockedFallTest=420` completed with no console errors, `chassisOverlappingDynamicBlocks:0`, `chassisRestingDynamicBlocks:2`, `structuralDebrisNearDozer:27`, `dynamicDebris:52`, `independentPhysicalBricks:11`, `settledVisualDebris:27`, and `floatingSettledDebris:0`; screenshot shows reduced per-block spin/fan-out while preserving bulldozer collision.

Latest note 2026-06-17: Reduced the slow-motion/sticky feel when the falling wall reaches the bulldozer. Root cause was the dozer contact path creating too many live physics bodies and damping structural wall debris almost to zero while it rested on the chassis/blade. Structural wall debris near the bulldozer no longer fractures into many independent bricks, the dozer/blade additional solver iterations were lowered from `8` to `2`, the dozer separation helper now skips far-away rubble, top-rest damping for structural wall debris preserves more horizontal motion, and non-near structural debris settles sooner. Live dynamic debris cap was reduced to `36`. Debug output now includes `structuralDebrisNearDozer`. Verification: `npm.cmd run build` passes; required develop-web-game smoke `?v=dozer-contact-no-slow-cap36&bulldozerBlockedFallTest=420` completed with no console errors, `chassisOverlappingDynamicBlocks:0`, `chassisRestingDynamicBlocks:1`, `structuralDebrisNearDozer:31`, `dynamicDebris:54`, `independentPhysicalBricks:11`, `settledVisualDebris:25`, and `floatingSettledDebris:0`. Earlier same scenario had `dynamicDebris:102` and `independentPhysicalBricks` much higher, which caused the user-visible slowdown.

Latest note 2026-06-17: Added an H-panel reset button and retuned the wall lean after feedback that the wall leaned too slowly and skipped to the end around 80%. The H tuning panel now includes a top-level `Reset Scene` button that calls `resetPrototype()` and refocuses the canvas. Default `criticalBearingLeanSpeed` was raised from `0.08` to `0.14`, settings storage bumped to `v8`, and the `lean speed` live slider now correctly allows the full `0.05-1.45` range instead of clamping back to `0.15`. Release thresholds were pushed closer to full floor-flat (`forwardPanelLean > 1.48`, `sidePanelLean > 1.06`), the bulldozer blocker no longer triggers until `panelLean >= 1.28`, and the fallback wait was extended to delay+1000 frames so the panel does not end at the 80% visual mark. Verification: `npm.cmd run build` passes; required develop-web-game smoke `?v=reset-lean-mid&criticalSideLeanTest=320` showed mid-animation still attached with `leanSpeed:0.14`, `leftBlocks:43`, `structuralWallReleases:0`, `fracturedBlocks:0`, `faceForwardLeanAngles.left:-1.096`, and no console errors; late smoke `?v=reset-lean-late&criticalSideTest=900` completed with `leftBlocks:0`, `structuralWallReleases:43`, `faceForwardLeanAngles.left:-1.5`, `floatingSettledDebris:0`, and no console errors. A direct ad-hoc H-panel text check could not import Playwright from this repo, but the GUI code compiles.

Latest note 2026-06-17: Fixed two follow-up collapse regressions: roof slabs exploding upward during wall fall, and the leaning animation ending almost instantly at about 10% lean. Collapsed roof slabs now get `roofCollapseDebris` metadata, gentler downward-only release velocity, stronger gravity/damping, capped angular velocity, a max collapse Y, and a per-step `stabilizeCollapsedRoofDebris()` pass before/after physics and after bulldozer separation. The bulldozer separator no longer injects positive Y velocity into roof-collapse debris. Debug output now reports `roofCollapseDebris`, `roofCollapseMaxY`, and `roofCollapseMaxUpVelocity`. The lean skip came from `isBulldozerBlockingFallingFace()` triggering at `0.42` radians and bypassing the delay entirely; blocker release now requires at least `0.95` radians and still requires the critical lean delay. Bumped settings storage to `v7` so older saved fast lean tuning does not persist. Verification: `npm.cmd run build` passes; required develop-web-game smoke `?v=lean-no-skip-mid&criticalSideLeanTest=320` shows mid-animation still attached with `leftBlocks:43`, `structuralWallReleases:0`, `fracturedBlocks:0`, `faceForwardLeanAngles.left:-0.515`, and no console errors; roof smoke `?v=roof-no-upkick-final&roofCollapseTest=220` shows `roofCollapseDebris:2`, `roofCollapseMaxUpVelocity:-0.35`, `floatingSettledDebris:0`, and no console errors.

Latest note 2026-06-17: Fixed the slowed wall panel hanging over/through the bulldozer before falling. Added `isBulldozerBlockingFallingFace()` so critical-bearing wall panels release into real dynamic physics as soon as their falling sweep overlaps the bulldozer chassis/cab/blade, overriding the lean delay instead of staying visual-only and clipping through the vehicle. Strengthened `separateDynamicEntityFromBox()` for structural wall debris with a larger top-rest window and enabled top-rest separation for chassis and cab as well as the blade. Debug output now includes `chassisOverlappingDynamicBlocks` and `chassisRestingDynamicBlocks`. Added `prototype_bulldozer_blocked_wall_fall_for_test()` / `?bulldozerBlockedFallTest=<frames>`. Verification: `npm.cmd run build` passes; required develop-web-game smoke at `http://127.0.0.1:5174/?v=bulldozer-blocked-fall-final&bulldozerBlockedFallTest=360` completed with no console error file, `chassisOverlappingDynamicBlocks:0`, `cabOverlappingDynamicBlocks:0`, `chassisRestingDynamicBlocks:1`, `structuralWallReleases:57`, `faceForwardLeanAngles.front:1.5`, and `floatingSettledDebris:0`, with screenshot showing rubble around/on the bulldozer instead of through it.

Latest note 2026-06-17: Slowed the critical-bearing wall lean after feedback that it now tries to lean but still happens too fast. Added `criticalBearingLeanSpeed` to settings with a slower default of `0.08`, sanitized/live-tuned range `0.05-1.45`, and a live H-panel `lean speed` slider in `Support and roof`. `updateWallGravitySag()` now uses that value for both critical lean damping and collapse accumulation, and critical-bearing release is shared through `isCriticalBearingPanelReleaseReady()` so the secondary leaning release path cannot drop/crumble wall bands before the near-floor angle. Extended the critical fall test hooks to `900` frames for the slower animation. Verification: `npm.cmd run build` passes; required develop-web-game smoke at `?v=very-slow-lean-mid&criticalSideLeanTest=320` showed slow mid-lean with `leanSpeed:0.08`, `leftBlocks:43`, `structuralWallReleases:0`, `fracturedBlocks:0`, `faceForwardLeanAngles.left:-0.523`, and no console error file; late smoke at `?v=very-slow-lean-late-visual&criticalSideLeanTest=760` completed release with `leftBlocks:0`, `structuralWallReleases:43`, `faceForwardLeanAngles.left:-1.5`, `floatingSettledDebris:0`, and no console error file.

Latest note 2026-06-17: Fixed early crumble during the wall-panel floor fall. The attached panel lean previously capped around `0.64` radians, so the wall could release/fracture while still visibly upright; structural debris also had an "upright stuck" shatter path that could break slabs during the lean. Forward panel lean now reaches `1.5` radians, visual rotation clamps match that near-floor angle, flat-fall release waits for `1.42` radians (or a longer fallback), and structural-collapse wall slabs only fracture once they are almost horizontal (`uprightness < 0.16`) and on the ground. Verification: `npm.cmd run build` passes; required develop-web-game smoke at `http://127.0.0.1:5174/?v=full-floor-lean-smoke&criticalSideLeanTest=320` completed with no console error file, `faceForwardLeanAngles.left:-1.5`, `structuralWallReleases:43`, `fracturedBlocks:30`, and `floatingSettledDebris:0`.

Latest note 2026-06-17: Fixed the critical-bearing lean delay so maxing the H-panel slider is actually visible during manual play. The delay was previously allowed to count up while the bulldozer was still chewing the bottom wall, so by the time the user backed away the delay could already be spent. Wall contact now records `lastBearingContactStep`, and `updateWallGravitySag()` only advances `criticalBearingSteps` after bearing contact has been quiet for 24 simulation steps. The `lean delay frames` slider also live-updates active tuning immediately, so maxing it to `300` takes effect without Apply/reload. Verification: `npm.cmd run build` passes; browser check opened the H panel, set `lean delay frames` to `300`, and debug payload immediately reported applied/draft `criticalBearingDelayFrames:300`; the critical side bearing hook still completed collapse with `leftBlocks:0`, `structuralWallReleases:43`, and `floatingSettledDebris:0`. Required develop-web-game smoke at `http://127.0.0.1:5174/?v=delay-live-final` loaded cleanly with no console error file.

Latest note 2026-06-17: Added a live H-panel slider for the critical-bearing lean delay after the user asked to tune the delay value. `SettingsValues` now includes `criticalBearingDelayFrames` with default `150`, saved settings sanitize it in the `30-300` frame range, `getCriticalBearingDelayFrames()` drives the release gate, and the H tuning panel's `Support and roof` folder now shows `lean delay frames` with step `5`. Debug payload reports `criticalBearingDelayFrames` and `draftCriticalBearingDelayFrames`. Verification: `npm.cmd run build` passes; required develop-web-game smoke at `http://127.0.0.1:5174/?v=delay-slider-check` completed with no console error file; Playwright opened H panel and confirmed the GUI text contains `lean delay frames` and the debug payload reports applied/draft value `150`.

Latest note 2026-06-17: Increased the critical-bearing lean delay after feedback to "ADD MORE DELAY." Critical lean damping is now slower (`1.45` instead of `2.4`) and release waits `150` simulation steps instead of `90`, so the wall spends longer visibly tipping before the flat-fall release. Verification: `npm.cmd run build` passes; required develop-web-game smoke at `http://127.0.0.1:5174/?local=1&panel-lean-fix=1&criticalSideTest=520` completed with no console error file, final state still releases the failed side wall with `faceBlocks.left:0`, `structuralWallReleases:43`, `floatingSettledDebris:0`, and no hanging panel.

Latest note 2026-06-17: Slowed the critical-bottom collapse so it visibly leans before releasing, after feedback that it now broke straight or leaned too fast. The critical bearing path no longer snaps `stress.lean` to `0.98` immediately; `updateWallGravitySag()` ramps critical lean with damping and `releaseUnsupportedWallBlocks()` waits about 90 simulation steps before converting the tilted face into falling debris. Debug output now includes `criticalBearingSteps`. Verification: `npm.cmd run build` passes; required develop-web-game smoke at `http://127.0.0.1:5174/?local=1&panel-lean-fix=1&criticalSideTest=520` completed with no console error file, final screenshot still shows collapse without hanging, `faceBlocks.left:0`, `structuralWallReleases:43`, `floatingSettledDebris:0`, and `unsupportedFixedBlocks:0`.

Latest note 2026-06-17: Fixed the still-hanging side/front wall case by broadening critical bearing failure beyond the earlier too-narrow front-face test. Side walls have only 6 bottom columns, so one remaining bottom column is 16.7%; the prior 16% cutoff missed it. Critical collapse now triggers at 34% or less bottom bearing, or when a long continuous bottom span is missing, so a wall with only a small bottom remnant cannot bridge and hang. Added `getFaceFoundationMissingRunRatio()` and `prototype_critical_side_bearing_fall_for_test()` / `?criticalSideTest=<frames>`. Verification: `npm.cmd run build` passes; required develop-web-game smoke at `http://127.0.0.1:5174/?local=1&panel-lean-fix=1&criticalSideTest=460` completed with no console error file, screenshot showing the side wall gone into rubble instead of hanging, bulldozer backed up to `z:30.73`, `faceBlocks.left:0`, `faceStress.left.foundationRatio:0`, `structuralWallReleases:43`, `floatingSettledDebris:0`, and `unsupportedFixedBlocks:0`.

Latest note 2026-06-17: Added the critical bottom-bearing collapse rule after feedback that destroying ~90% of the bottom should make the wall fall when backing the bulldozer away, without requiring the final ~10% to be manually destroyed. `isWallFaceCriticallyBearingFailed()` now treats a face as failed when the foundation row is at or below 16% support, bypassing the old high-lean/broad-collapse wait and immediately releasing the whole face through the flat-fall path. Added `prototype_critical_bottom_bearing_fall_for_test()` and `?criticalBottomTest=<frames>`, which leaves one bottom column standing, backs the bulldozer up, and verifies collapse. Verification: `npm.cmd run build` passes; required develop-web-game smoke at `http://127.0.0.1:5174/?local=1&panel-lean-fix=1&criticalBottomTest=420` completed with no console error file, screenshot showing the front wall collapsed into floor rubble, bulldozer backed up to `z:30.64`, `faceBlocks.front:0`, `faceBlocks.roof:0`, `structuralWallReleases:57`, `chippedWallSlabs:48`, `floatingSettledDebris:0`, and `unsupportedFixedBlocks:0`.

Latest note 2026-06-17: Added a second anti-hanging safeguard for released wall slabs after the user still saw a vertical hanging panel in manual play. `driveStructuralWallDebrisTowardFlatFall()` now runs inside the physics step and keeps applying floor-fall torque, outward velocity, and extra gravity to released structural wall debris. It also covers loose `house-*` masonry debris that has already lost `wallBlock` metadata, so side/front slabs cannot escape the flat-fall rule. `processDynamicWallFractures()` now shatters upright/stuck structural slabs instead of letting them remain as vertical sheets. Verification: `npm.cmd run build` passes; required develop-web-game smoke at `http://127.0.0.1:5174/?local=1&panel-lean-fix=1&flatPanelFallTest=520` completed with no console error file, screenshot showing the front panel no longer hanging, `faceBlocks.front:0`, `faceBlocks.roof:0`, `floatingSettledDebris:0`, `unsupportedFixedBlocks:0`, `airborneDynamicDebris:4`, and `wallPiecesBroken:142`.

Latest note 2026-06-17: Forced over-leaned wall panels to fall flat instead of hanging like hinged/static sheets. The release path now treats a steep forward or side panel angle as a hard flat-fall condition, releases every remaining attached block on that face in one coherent pass, syncs each new dynamic body to the current leaned visual pose before detaching, and applies stronger outward/downward velocity plus panel-fall spin so the masonry drops onto the ground. Added `prototype_flat_panel_fall_for_test()` and `?flatPanelFallTest=<frames>`. Verification: `npm.cmd run build` passes; required develop-web-game smoke at `http://127.0.0.1:5174/?flatPanelFallTest=360` and the user's old query shape `http://127.0.0.1:5174/?local=1&panel-lean-fix=1&flatPanelFallTest=360` both completed with no console error file, screenshots showing the front wall no longer hanging, `faceBlocks.front:0`, `faceBlocks.roof:0`, `structuralWallReleases:40`, `chippedWallSlabs:36`, and `floatingSettledDebris:0`.

Latest note 2026-06-17: Removed the localhost-to-GitHub-Pages redirect from `index.html` after the user reported that typing `http://127.0.0.1:5174/` opened the old public Pages version. Local dev URLs now stay local without needing `?local=1`; the public Pages URL is no longer forced from localhost. Verification: `npm.cmd run build` passes; `Invoke-WebRequest -MaximumRedirection 0 http://127.0.0.1:5174/` returns status `200` with final URI still `http://127.0.0.1:5174/`; required develop-web-game smoke against the bare local URL completed in `output/local-root-no-redirect-smoke/` with no console error file and screenshot showing the local prototype loaded.

Latest note 2026-06-17: Hardened panel tip release so steep wall panels cannot remain hanging. The prior tip-over release still left loopholes: it required the face to also pass broad/lateral instability checks, and the release band originally skipped the foundation row, leaving hinge-like leftovers. `releaseFaceCollapseBand()` now includes row 0 when called for a full face tip-over, and both release paths now treat either forward panel pitch or side panel roll as enough to force release once lean/support thresholds are crossed. Verification: `npm.cmd run build` passes; required develop-web-game smoke completed in `output/hard-tip-release-smoke/` with no console error file, screenshot showing no hanging front slab and rubble on the ground, `faceBlocks.front:0`, `fracturedBlocks:36`, `chippedWallSlabs:36`, `dynamicDebris:55`, `wallPiecesBroken:174`, `faceForwardLeanAngles.front:0.64`, and `floatingSettledDebris:0`.

Latest note 2026-06-17: Fixed steep outward-leaning front panels hanging in place instead of actually falling. The panel could reach a large shared forward pitch but still wait on the broader collapse gate, leaving it visually stuck like a hinged wall. Added a `panelTipOverReady` gate in both `releaseUnsupportedWallBlocks()` and `releaseLeaningWallBlocks()`: once the forward panel angle is steep enough, lean is high, and support is low, the whole remaining face band releases as dynamic structural debris with stronger outward fall impulse. Verification: `npm.cmd run build` passes; required develop-web-game smoke completed in `output/panel-tip-release-smoke/` with no console error file, screenshot showing the previous slab gone and rubble on the ground, `faceBlocks.front:2`, `fracturedBlocks:26`, `chippedWallSlabs:26`, `dynamicDebris:60`, `wallPiecesBroken:154`, `faceForwardLeanAngles.front:0.64`, and `floatingSettledDebris:0`; focused browser check in `output/panel-tip-release-focused/` also had zero browser errors with `frontBlocks:2`, `fracturedBlocks:26`, `dynamicDebris:67`, and `floatingSettledDebris:0`.

Latest note 2026-06-17: Corrected front wall panel fall direction so it pitches outward instead of inward. The previous front-forward panel pitch reused `getFaceFallDirection('front')`, which points into the house (`-Z`) for older internal collapse logic. Added `getFaceLowResistanceDirection()` for exterior/least-resistance fall: front now uses `+Z`, back `-Z`, and side faces point outward from the shell. `releaseFaceCollapseBand()` and `getWallFaceForwardPanelLeanAngle()` now use the low-resistance direction, so released front panels get outward velocity and outward shared panel pitch. Verification: `npm.cmd run build` passes; required develop-web-game smoke completed in `output/outward-front-fall-smoke/` with no console error file, screenshot showing rubble falling outside the front face, `faceForwardLeanAngles.front:0.64` (was negative/inward), `fracturedBlocks:29`, `chippedWallSlabs:29`, `wallPiecesBroken:156`, `floatingSettledDebris:0`, and `unsupportedFixedBlocks:0`.

Latest note 2026-06-17: Added least-resistance forward fall and impact shatter to panel lean. The previous panel lean only rolled sideways, so a front wall with a blown-out front opening did not pitch outward through the low-resistance path. Added `getWallFaceForwardPanelLeanAngle()` and applied a second shared forward/outward hinge rotation around the same bottom pivot, with debug output `faceForwardLeanAngles`. Structural-collapse panels now keep their wall-block metadata, reset their release age, get stronger outward velocity/angular velocity, and remain eligible for `processDynamicWallFractures()` after release. Dynamic fracture now includes released structural-collapse candidates and triggers on ground impact, with a slightly higher forced brick conversion budget so slabs can break into masonry pieces instead of staying whole. Verification: `npm.cmd run build` passes; required develop-web-game smoke in `output/front-fall-impact-smoke/` had no console error file and state showed `faceForwardLeanAngles.front:-0.64`, `fracturedBlocks:24`, `chippedWallSlabs:24`, `wallPiecesBroken:148`, and `floatingSettledDebris:0` (the client canvas screenshot was black, so a full-page Playwright verification was also captured in `output/front-fall-impact-focused/page.png` with zero browser errors, `faceForwardLeanAngles.front:-0.64`, `fracturedBlocks:19`, and visible broken front rubble).

Latest note 2026-06-16: Reworked wall lean so it behaves like a connected wall face instead of individually rotated brick objects. The previous angle pass applied per-block row rotation/lateral sag, which made each brick/object look independently twisted. Standing stressed wall blocks now use one shared `getWallFacePanelLeanAngle()` and bottom hinge pivot from `getWallFaceLeanPivot()`, applying the same face-panel transform to all still-attached blocks. Row lateral sag was removed; only downward sag remains, while broad release waits until after the panel lean phase. Debug output now includes `facePanelLeanAngles`. Verification: `npm.cmd run build` passes; required develop-web-game smoke completed in `output/panel-lean-smoke/` with no console error file, screenshot showing the remaining front wall section leaning as one angled panel, `facePanelLeanAngles.front:0.45`, `faceBlocks.front:24`, `faceStress.front.direction:-1`, `floatingSettledDebris:0`, and `unsupportedFixedBlocks:0`.

Latest note 2026-06-16: Made wall lean read as an actual left/right angled failure instead of mostly vertical sag. Symmetric foundation loss could leave `faceStress.direction` at `0`, so the wall had lean/collapse values but no lateral fall direction. Added `getFallbackLeanDirection()` to pick a deterministic side from broken/standing foundation distribution, and made foundation lean assign that direction when imbalance is neutral. Strengthened row-height lateral sag and direct per-block angular rotation so upper rows visibly shear/tilt more than lower rows. Delayed broad structural release thresholds so the wall has a visible angled-lean phase before bands drop. Added `?angleLeanTest=<frames>` for quick visual capture. Verification: `npm.cmd run build` passes; focused probes in `output/angle-lean-focused/` show `faceStress.front.direction:-1` with zero browser errors; required develop-web-game smoke completed in `output/angle-lean-smoke/` with no console error file, screenshot showing the front face diagonally leaning left before collapse, `faceBlocks.front:24`, `faceStress.front.lean:0.984`, `faceStress.front.direction:-1`, `maxGravitySag:0.216`, `saggingBlocks:22`, `floatingSettledDebris:0`, and `unsupportedFixedBlocks:0`.

Latest note 2026-06-16: Fixed floating brick/debris visuals after collapse. Root cause was the settling path using `hasGroundOrBodySupport()` for visual-freezing decisions, so debris could be converted into bodyless settled visuals while temporarily resting on another falling wall or roof piece; when that support moved away, the visual mesh stayed suspended in the air. Added `hasGroundSupportForSettling()` so debris only becomes settled visual debris when actually near the ground plane, while ordinary body support can still be used for airborne diagnostics. Added `floatingSettledDebris` to debug output. Verification: `npm.cmd run build` passes; required bottom/foundation collapse smoke completed in `output/no-floating-settle-smoke/` with no console error file, valid screenshot, `floatingSettledDebris:0`, `settledVisualDebris:77`, `dynamicDebris:20`, `airborneDynamicDebris:12` (still live physics, not frozen visuals), `structuralWallReleases:56`, and `unsupportedFixedBlocks:0`.

Latest note 2026-06-16: Fixed leaning/support collapse looking like a left-to-right brick deletion wipe. Root cause was `releaseUnsupportedWallBlocks()` and `releaseLeaningWallBlocks()` releasing one sorted wall block at a time under a small per-frame budget, then routing each release through the impact fragmentation path; visually this scanned across a row, then the next row. Added a structural-collapse release path that keeps each released wall block as visible masonry debris, and changed broad lean collapse to release coherent face bands/rows together via `releaseFaceCollapseBand()` instead of single sorted blocks. Structural-collapse debris is tagged and now gets a much longer live-physics grace period before settling, so the material stays visible during the lean moment. Verification: `npm.cmd run build` passes; focused asymmetric lean probe in `output/lean-band-collapse-focused/` had zero browser errors, screenshot `shot-early.png` shows a rough collapsed opening instead of a clean row wipe, with `structuralWallReleases:38`, `dynamicDebris:60`, `activeDynamicBodies:60`, and `unsupportedFixedBlocks:0`; required develop-web-game smoke completed in `output/lean-band-collapse-smoke/` with no console error file, valid screenshot, visible base rubble, `structuralWallReleases:38`, `wallPiecesBroken:112`, and `unsupportedFixedBlocks:0`.

Latest note 2026-06-16: Fixed wall pieces popping out and disappearing during heavy destruction. Root cause was the wall-block fragmentation path partially converting a block into independent bricks when the live debris budget was tight, then deleting the original source block; fallback shatter paths also removed blocks outright when conversion failed. `replaceWallBlockWithIndependentBrickBodies()` now requires full conversion or returns false, and both broken/shatter fallback paths use `releaseWallBlockAsVisibleDebris()` to preserve the original masonry block as falling visible debris instead of deleting it. Debris settling now waits longer and stays dynamic nearer the bulldozer so fresh collapse rubble does not immediately get flattened into the visual-only pool. Verification: `npm.cmd run build` passes; heavy bottom-collapse hook saved `../output/no-popout-delete-heavy-collapse.png` with the lower wall broken open and material still visible, reporting `wallPiecesBroken:129`, `dynamicDebris:19`, `settledVisualDebris:119`, `reactivatableDebris:112`, and `independentBrickVisuals:267`; required develop-web-game smoke completed in `output/no-popout-delete-smoke/` with no console error file, valid screenshot, `wallPiecesBroken:48`, `independentPhysicalBricks:40`, `dynamicDebris:42`, and `settledVisualDebris:22`.

Latest note 2026-06-16: Fixed side walls being erased after only a few touches. The previous foundation-collapse threshold treated a 50% damaged foundation row as catastrophic, which is too sensitive on the short 6-column left/right walls. Added `getFoundationCollapseThreshold()` and lowered the catastrophic foundation threshold to `0.26`; foundation loss only overrides lower-wall support once it is genuinely near-total, while normal local damage still creates local holes. Added `prototype_side_wall_bump_resilience_for_test()` and `?sideResilienceTest=1`. Verification: `npm.cmd run build` passes; focused side smoke in `output/web-game-side-wall-resilience-smoke/` has no browser error file, `faceBlocks.right:45` from `48`, `foundationRatio:0.5`, `lean:0`, `structuralWallReleases:0`, and a screenshot showing a local side-wall bite instead of a vanished side. Re-ran bottom support collapse in `output/web-game-bottom-foundation-collapse-after-side-fix/`, still no browser errors with `faceBlocks.front:0`, `faceBlocks.roof:0`, `structuralWallReleases:56`, so true foundation loss still collapses. Baseline drive smoke in `output/web-game-side-threshold-regression-smoke/` has no browser error file and preserves normal breach behavior with `structuralWallReleases:0`.

Latest note 2026-06-16: Fixed the case where a wall could stay upright after almost all bottom/foundation support was removed. The support solver now tracks the true foundation row separately from the broader lower-wall support ratio, so a face with its bottom bearing row gone becomes broadly undermined even if rows 1-3 still exist. Symmetric foundation loss now creates wall-face stress with `foundationRatio:0`, sag/lean while still attached, then releases the upper face/roof after a short stress delay instead of holding a static billboard. Added `prototype_bottom_foundation_collapse_for_test(frames?)` and `?bottomSupportTest=1`. Verification: `npm.cmd run build` passes; early 20-frame probe in `output/bottom-foundation-midcheck-20-final/` has no browser errors, `foundationRatio:0`, `lean:0.865`, `saggingBlocks:48`, `structuralWallReleases:0`, proving the attached wall starts sagging before release. Full focused smoke in `output/web-game-bottom-foundation-collapse-final/` has no browser error file, `faceBlocks.front:0`, `faceBlocks.roof:0`, `structuralWallReleases:56`, `unsupportedFixedBlocks:0`, and the front face no longer remains magically supported. Baseline drive smoke in `output/web-game-foundation-collapse-regression-smoke/` has no browser error file and preserves normal breach behavior without random full collapse.

Latest note 2026-06-16: Made wall damage respect bulldozer rotation while the vehicle is already inside/against the house. `updateBulldozerControls()` now tracks per-frame yaw speed, and `processVehicleDamage()` converts stationary A/D rotation into tangential contact speed with extra front/rear/side probes, so the blade/body corners damage masonry while turning instead of clipping through static wall blocks. Added `prototype_stationary_turn_wall_damage_for_test()` and `?stationaryTurnTest=1` for repeatable verification. Verification: `npm.cmd run build` passes; focused required smoke completed in `output/web-game-stationary-turn-wall-smoke/` with no browser error file, zero linear bulldozer speed, `impactEvents:89`, `wallPiecesBroken:120`, `independentPhysicalBricks:91`, `unsupportedFixedBlocks:0`, and screenshot showing side/corner turning contact breaking the wall. Baseline regression smoke completed in `output/web-game-turn-damage-regression-smoke/` with no browser error file, normal breach behavior, `wallPiecesBroken:63`, and `unsupportedFixedBlocks:0`.

Latest note 2026-06-16: Made the inner wall faces use the same brick-instance skin as the outside. Physical house blocks now call `createIntactBrickWallVisual()` with `doubleSided:true`, so exposed interior surfaces around holes render matching brick courses instead of the plain mortar/base color. Verification: `npm.cmd run build` passes; required smoke completed in `output/web-game-inner-brick-skin-smoke/` with no browser error file and `intactBrickVisuals:1134`, confirming the standing wall brick-face instances are present on both sides.

Latest note 2026-06-16: Added asymmetric lower-support stress so a front wall no longer stays perfectly upright after most of the bottom support is destroyed on one side. Each wall face now tracks lower support ratio, support imbalance, lean direction, lean amount, and collapse progress. `updateWallGravitySag()` applies a persistent visual lean/sag toward the weak side, `simulationNeedsStep()` keeps ticking while face stress is active, and `releaseLeaningWallBlocks()` gradually releases weak-side blocks once lean/collapse gets severe. Debug output now includes `wall.faceStress`. Added `prototype_asymmetric_front_support_lean_for_test()` and `?asymmetricLeanTest=1` for the exact case. Verification: `npm.cmd run build` passes; required focused smoke completed in `output/web-game-asymmetric-lean-smoke/` with no browser error file, `faceStress.front.direction:-1`, `faceStress.front.imbalance:0.804`, `faceStress.front.lean:1`, `structuralWallReleases:38`, and the front wall failing toward the left-side gap instead of remaining upright.

Latest note 2026-06-16: Restored visible inward swelling around wall openings without reintroducing floating fixed blocks. The per-frame body-to-mesh sync now reapplies visual wall deformation so dents are not erased after impact. Wall deformation now uses a controlled inward-only visual offset based on the face inward direction, with a stronger cap (`maxBulge:0.78`), stronger local bow/tilt, and almost no outward offset. Physics colliders remain at the authored wall positions. Verification: `npm.cmd run build` passes; required smoke completed in `output/web-game-inward-wall-dent-strong-smoke/` with no browser error file, `maxBulge:0.78`, `unsupportedFixedBlocks:0`, `structural.unsupportedIslands:0`, and a valid screenshot showing the area around the opening denting inward instead of floating outward.

Latest note 2026-06-16: Fixed roof pieces exploding outward after support loss. The roof failure path no longer fragments each roof half into a fast grid of small chunks; each half now collapses as one heavy slab with no horizontal impulse, only downward velocity, low spin, and heavier gravity. Added `prototype_roof_collapse_for_test()` plus `?roofCollapseTest=1` for repeatable roof verification. Verification: `npm.cmd run build` passes; required smoke completed in `output/web-game-roof-collapse-slab-smoke/` with no browser error file, `faceBlocks.roof:0`, `wallPiecesBroken:2`, `dynamicDebris:0`, `airborneDynamicDebris:0`, and `settledVisualDebris:2`. Full-page visual check saved at `output/roof-collapse-slab-fullpage/fullpage.png`.

Latest note 2026-06-16: Tuned masonry debris so broken bricks no longer drift down like leaves. Independent brick, wall-chunk fragment, and grouped row-release paths now use lower linear/angular damping, `gravityScale:1.35`, stronger initial downward velocity, and reduced random flutter spin. This keeps broken bricks reading as dense masonry while preserving some tumble. Verification: `npm.cmd run build` passes; required smoke completed in `output/web-game-heavy-brick-fall-smoke/` with no browser error file, valid screenshot, `dynamicDebris:42`, only `airborneDynamicDebris:5`, `maxBulge:0.42`, and `unsupportedFixedBlocks:0`.

Latest note 2026-06-16: Fixed the swollen in/out floating wall-block artefact reported from the local structural graph build. Root cause was `applyWallBulge()` still translating fixed Rapier wall bodies to `home + bulge`, which made impacted supported masonry hover out of the facade as shelf-like blocks. Wall bulge is now visual-only: fixed bodies stay at their authored/home positions, the bulge cap is reduced to `0.42`, and `applyWallBlockVisualDeformation()` resets from the current body pose before applying a small bounded scale/tilt instead of accumulating rotations. Verification: `npm.cmd run build` passes; required smoke completed in `output/web-game-bulge-visual-only-smoke/` with no browser error file, valid screenshot, `maxBulge:0.42`, `structural.unsupportedIslands:0`, graph-based `unsupportedFixedBlocks:0`, and the old floating shelf look gone.

Latest note 2026-06-16: Started the requested realistic destruction architecture pass locally only, with no push/publish. Root causes found: the bulldozer is still kinematic-position driven, wall bulge/fake sag moved fixed bodies and created visual/collider gaps, column collapse was using row/column support heuristics instead of support islands, and FPS drops are from bursty independent brick/roof debris plus secondary scans. Added a first structural graph slice in `src/main.ts`: material profiles, structural nodes, mortar/roof bonds, deterministic bond randomness, tension/compression/shear damage buckets, support island solving, bond severing on block fracture/removal, and debug graph stats. Unsupported wall release now checks graph unsupported islands so locally holed masonry can still carry through side bonds instead of dropping an entire column. Added `docs/destruction-architecture-note.md` with the exact causes and remaining vehicle/contact/fracture/performance work. Verification: `npm.cmd run build` passes; required `develop-web-game` smoke completed in `output/web-game-structural-graph-smoke/` with no browser error file, valid screenshot, `436` structural bonds, `56` broken bonds after impact, `1` supported island, `0` unsupported islands, `8` direct vertical support gaps, and graph-based `unsupportedFixedBlocks:0`.

Latest note 2026-06-16: Corrected the reverse wall-contact fix after the real screenshot showed the bulldozer backing into the right side wall with the rear/side of its body. The first rear probe reached backward but was too narrow (`halfRight:1.58`) compared with the doubled bulldozer track/body width, so rear-side contact could visibly touch masonry without entering the damage volume. Rear reverse damage now uses full chassis width (`bulldozer.halfExtents.x + 0.5`) and the turning rear probe uses the same wide width. Added `prototype_side_rear_reverse_wall_contact_for_test()` to match the screenshot case: blade points away from the house, rear backs into the right wall. Verification: `npm.cmd run build` passes; focused browser hook reports side rear reverse contact with `impactEventsDelta:48`, `wallPiecesDelta:39`, and right wall blocks dropping from `48` to `32`; centered rear reverse still reports `impactEventsDelta:39`, `wallPiecesDelta:30`; general reverse still breaks walls (`wallPiecesBroken:48`). Required develop-web-game smoke completed in `output/side-rear-reverse-contact-smoke/` with valid screenshot, `impactEvents:18`, `wallPiecesBroken:8`, `wallBreaches:1`, and no console error file.

Latest note 2026-06-16: Fixed rear-first reverse wall contact after feedback that going backwards did not register with walls. Root cause was the rear damage probe sitting too far inside the chassis, so the visible back of the bulldozer could touch masonry before the damage volume reached it. Moved the rear probe to the real rear bumper, widened/lengthened it slightly, strengthened reverse rear damage, and added `prototype_rear_reverse_wall_contact_for_test()` for regression coverage. Verification: `npm.cmd run build` passes; focused browser hook reports rear-first reverse contact with `impactEventsDelta:32` and `wallPiecesDelta:20`; existing reverse hook still breaks walls (`wallPiecesBroken:48`); forward drive smoke still breaks walls (`wallPiecesBroken:42`); required develop-web-game smoke completed in `output/rear-reverse-contact-smoke/` with visible damage screenshot `shot-0.png`, `impactEvents:16`, `wallPiecesBroken:18`, and no console errors reported by the page.

Latest note 2026-06-16: Forced quality to High, removed the quality option from the H panel, and removed interior partition walls from the compact house. Defaults now set `quality:'High'`, settings storage is v6, saved settings sanitize quality back to High, `applyDraftSettings()` enforces High, and debug output no longer reports `draftQuality`. `createHouseInterior()` and the interior partition builder were removed from normal runtime; reset now reports `roomPartitions:0` and the house is an empty exterior shell. Verification: `npm.cmd run build` passes; required smoke completed in `output/web-game-quality-high-no-interior-smoke/`; focused panel probe reports `hasQualityControl:false`, `hasWallControls:true`, applied `quality:'High'`, and `roomPartitions:0`, with screenshot `output/quality-high-no-interior-panel.png`.

Latest note 2026-06-16: Forced rendering quality to High and removed the quality dropdown from the H tuning panel. Defaults now set `quality: 'High'`, settings storage bumped to v6, saved settings sanitize quality back to High, and `applyDraftSettings()` enforces High so older Low/Medium browser settings cannot persist. Debug output no longer reports `draftQuality`. Verification: `npm.cmd run build` passes; required `develop-web-game` smoke completed in `output/web-game-quality-high-panel-smoke/`; focused panel probe reports `hasQualityControl:false`, `hasWallControls:true`, and applied `quality:'High'`, with screenshot `output/quality-high-panel-open.png`.

Latest note 2026-06-16: Reconfigured the H tuning panel for the current compact house scenario after the bridge was removed and feedback showed light brick pushes could collapse too much of a vertical column. The panel is now titled `House Destruction Tuning` and replaces bridge/column controls with wall-specific sliders: `brick toughness`, `blade damage scale`, `rubble hit threshold`, `debris throw speed`, `wall support loss`, and `roof drop support`, plus the remaining bulldozer/quality controls. Bumped settings storage to v5 so old bridge-era browser tuning does not carry forward. Defaults are less fragile: wall break damage is now `16`, blade damage scale `0.72`, secondary rubble threshold `42`, support release ratio `0.38`, and roof drop ratio `0.34`. Verification: `npm.cmd run build` passes; required smoke screenshot `output/web-game-house-tuning-panel-smoke/shot-0.png`; open-panel screenshot `output/house-tuning-panel-open.png` shows no bridge/column controls and the new wall controls; gentle push probe breaks only `1` wall piece with no structural releases (`output/gentle-wall-push-tuned.png`), while a committed push still breaches the building (`wallPiecesBroken:168`, `output/strong-wall-push-tuned.png`), with no console errors.

Latest note 2026-06-16: Removed the bridge prop from normal gameplay, made the compact building taller, and fixed roof panels staying as fixed/floating slabs after wall support is gone. The boot/reset path no longer creates the bridge, the bridge HUD lines hide when there are no bridge objects, and dead bridge creation code was removed. Added `houseHeightScale = 1.65`, increased wall rows from `6` to `8`, and raised physical lower rows from `3` to `4` while keeping the compact footprint. Roof release now drops both roof halves when either main face loses top/lower support, spawns roof chunks lower into the failing cavity, and gives them stronger downward velocity. Verification: `npm.cmd run build` passes; required `develop-web-game` smoke screenshot `output/web-game-no-bridge-taller-roof-final-smoke/shot-0.png` shows no bridge and no bridge HUD lines; focused roof-collapse probe has no console errors and reports `faceBlocks.roof:0` after front support collapse with screenshot `output/roof-drop-strong-no-bridge-probe.png`.

Latest note 2026-06-16: Reduced the house target to one-eighth of its previous 10x size so it is now only slightly larger than the doubled bulldozer. `houseScale` changed from `10` to `1.25`, the masonry grid was reduced from `20 x 14` front/back and `14 x 14` sides to `8 x 6` front/back and `6 x 6` sides, and physical lower rows dropped from `5` to `3`. Roof overhang now scales with the house (`0.48 * houseScale`) instead of using the old giant fixed `4.8` world-unit overhang. Verification: `npm.cmd run build` passes; required `develop-web-game` smoke screenshot `output/web-game-small-house-scale-smoke/shot-0.png` shows the house just bigger than the bulldozer; reset body count is now `217` with `170` wall blocks and `634` intact brick visuals; hit probe screenshot `output/small-house-hit-probe.png` confirms the bulldozer still breaks through the smaller house with no console errors.

Latest note 2026-06-16: Emergency-stabilized the failed gravity-sag/bridge experiment after screenshots showed giant static wall strips, sliding slab glitches, and FPS dropping to zero during heavy front-wall destruction. Fixed-body gravity sag is now disabled again (sag state resets to zero and no longer translates wall colliders), the unsupported-span bridge hold was removed so unsupported wall blocks release instead of hanging as broad static panels, live dynamic debris budget dropped from `220` to `120`, forced independent-brick breakup is capped to the remaining debris budget, and over-budget rubble settles much sooner. Verification: `npm.cmd run build` passes; required `develop-web-game` smoke completed in `output/web-game-fps-glitch-fix-smoke/`; focused wide-gap probe has no console errors, `maxGravitySag:0`, `saggingBlocks:0`, `unsupportedFixedBlocks:0`, and dynamic debris settles from `57` to `14` with screenshot `output/fps-glitch-gap-stabilized.png`; exact fallen brick size regression still reports `matches:true` with identical expected/actual brick sizes and screenshot `output/fps-fix-brick-size-regression.png`.

Latest note 2026-06-16: Corrected the first gravity-sag implementation after it visibly overcorrected into giant sliding/grey rectangular gaps. Sag is now capped to a subtle `0.62` world units in state and only applies `42%` of that to fixed-body translation, with fast upward fade (`18%` propagation) and much smaller span/depth coefficients. The bridge exemption now only holds the first three rows above a missing support span and only for spans up to `72%` of the face width, preventing tall unsupported slabs from hanging as static panels. Verification: `npm.cmd run build` passes; focused wide-gap probe reported `maxGravitySag:0.452`, `saggingBlocks:34`, `unsupportedFixedBlocks:34`, no console errors, and screenshot `output/large-gap-gravity-sag-fixed.png` no longer shows the huge upper rectangular void; required `develop-web-game` smoke passed in `output/web-game-gravity-sag-fixed-smoke/`.

Latest note 2026-06-16: Added gravity sag for fixed masonry above large broken wall gaps. Wall blocks now store a `sag` vector alongside impact bulge; support release allows anchored upper masonry to bridge a wide missing support span instead of immediately converting every unsupported upper block to debris. `updateWallGravitySag()` scans missing support spans, applies the strongest downward/outward sag at the middle of the gap, propagates some sag upward through rows, and exposes `maxGravitySag` plus `saggingBlocks` in debug state. Added `prototype_large_gap_sag_for_test()` for the exact wide-hole case. Verification: `npm.cmd run build` passes; required `develop-web-game` smoke passed with reset `maxGravitySag:0`, `saggingBlocks:0`, full wall counts, and no errors; wide-gap browser probe reported `maxGravitySag:2.217`, `saggingBlocks:94`, `unsupportedFixedBlocks:94`, and no console errors. Screenshot: `output/large-gap-gravity-sag.png`; smoke: `output/web-game-gravity-sag-smoke/shot-0.png`.

Latest note 2026-06-16: Made fallen wall bricks exactly match untouched facade brick dimensions. `replaceWallBlockWithIndependentBrickBodies()` now uses the same world-aligned clipped brick grid as `createIntactBrickWallVisual()` instead of recalculating a local per-block grid, and removes the extra chip scale from physical debris brick dimensions. Added `prototype_brick_size_match_for_test()` to compare expected untouched-grid cell sizes with actual generated physical brick body sizes. Verification: `npm.cmd run build` passes; browser hook reported `matches:true` with expected/actual sizes identical (`9` cells, including partial edge bricks such as `1.435 x 1.319 x 0.506` and full bricks `2.869 x 1.319 x 0.506`); required `develop-web-game` smoke passed with reset `bodyCount:1080`; broader partial-collapse check produced independent physical bricks with zero console errors. Screenshots: `output/exact-fallen-bricks-size-check.png`, `output/exact-fallen-bricks-collapse.png`, and `output/web-game-exact-bricks-smoke/shot-0.png`. Built Pages files were pushed to `ArisConstantinou/Game` main (`2d85e6b`) and gh-pages (`0b41fdb`), but the live GitHub Pages URL was still serving the previous `index-CfycVs6H.js` during the immediate post-push cache check.

Latest note 2026-06-16: Restored normal brick-sized destruction after feedback that the new all-physical wall grid made bricks look oversized compared with the earlier individual-brick version. The exterior wall grid is back to the finer `20 x 14` front/back and `14 x 14` side layout, with five physical lower support rows, a larger live debris budget, and a higher unsupported-release throttle so falling unsupported sections break into smaller independent masonry pieces instead of broad square panels. Verification: `npm.cmd run build` passes; required `develop-web-game` smoke passed against `http://127.0.0.1:5174/?local=1` with reset `bodyCount:1080`, front/back `280` blocks each, sides `196` blocks each, and no console errors; focused collapse probe reported collapse debris as `108-122` independent physical bricks. Screenshots: `output/web-game-brick-size-smoke/shot-0.png` and `output/brick-size-normal-collapse.png`.

Latest note 2026-06-16: Replaced the mixed wall architecture after feedback that lower walls and upper walls behaved differently, floated, and spawned different-sized falling pieces. The exterior house shell now uses one physical wall-block system from bottom to top; the old grouped/static upper-facade row path is no longer used for the main house walls. To keep this from becoming a thousand-body reset disaster, the wall grid was reduced uniformly to `10 x 8` on front/back faces and `8 x 8` on side faces, and unsupported wall-block releases are capped per step while support checks now run every dynamic-structure step instead of every fourth step. Verification: `npm.cmd run build` passes; reset body count dropped from `1080` in the first all-physical attempt to `416` with the reduced uniform grid; partial undermine test in `output/uniform-wall-grid-reduced-check.png` shows the top using the same physical breakup path as the lower wall with `staticVisualBlocks: 0`, `unsupportedStaticVisualBlocks: 0`, and no separate upper floating facade system remaining.

Latest note 2026-06-16: Tightened upper-wall support release after feedback that middle/top sections waited too long and still collapsed as giant row pieces. Root cause was twofold: grouped upper rows were recursively treating lower grouped rows as support, and lower-support ratio was computed only from surviving blocks, so missing lower blocks stopped counting against support. Fixed `getFaceLowerSupportRatio()` to measure against the full lower-wall grid, removed grouped-row-on-grouped-row support recursion, sorted unsupported static rows top-down, and made grouped structural releases prefer block-like row segments instead of long shelves. Verification: `npm.cmd run build` passes; `prototype_partial_front_support_collapse_for_test()` now reports `structuralWallReleases: 10` immediately (instead of 0) with `visualWallImpacts: 14`, `dynamicDebris: 69`, and screenshot `output/partial-top-release-check-3.png` shows upper rows starting to fall as separate segments once the lower front support is mostly gone; broader collapse screenshot `output/top-release-block-fragment-check-2.png` removes the old single-row shelf look.

Latest note 2026-06-16: Re-verified the disappearing middle/top wall fix live after the support-collapse feedback. A fresh local collapse run at `http://127.0.0.1:5174/?local=1&wall-support-ui-fix=1` keeps the released upper wall visible in `output/post-fix-support-collapse-live.png` instead of culling it; the debug payload after the collapse keeps `unsupportedStaticVisualBlocks: 0` with the upper facade still present as falling/slumped masonry. Rebuilt production output and copied it into the `game-main-pages` GitHub Pages mirror; local static verification at `http://127.0.0.1:4181/destruction-prototype/` shows the updated asset hash `index-CfycVs6H.js` and the same non-disappearing support-collapse behavior.

Latest note 2026-06-16: Fixed the disappearing middle/top wall bug after support-release tuning. Root cause was the static-wall release path removing grouped upper wall visuals before fragmentation, then returning `null` under debris pressure once the brick budget was exhausted, which made the released section vanish instead of fall. Unsupported upper/static rows now fall back to one dynamic released slab that reuses the original masonry mesh when full brick fragmentation is over budget. Verification: `npm.cmd run build` passes; partial support release now reports `structuralWallReleases: 10`, `unsupportedStaticVisualBlocks: 0`, and screenshot `output/partial-support-no-disappear.png` keeps the upper wall visible; broad front support collapse screenshot `output/broad-support-no-disappear.png` shows the released upper facade slumping forward instead of disappearing.

Latest note 2026-06-16: Replaced the mobile drive pad letter buttons with touch-native controls: forward/reverse are pedal icons and left/right are circular steering-wheel icons with accessible labels, while the action pad remains Q/E/G/X/C/T. Strengthened visible wall bowing by revealing impacted lower physical facade rows as soon as they deform, increasing wall bulge displacement, and adding per-block thickness/tilt so inward/outward hits read as swelling instead of only hidden collider offsets. Broad support loss now counts as lateral instability for upper visual rows: once a face loses most lower support, grouped upper rows release instead of staying as a straight floating slab. Added `prototype_partial_front_support_collapse_for_test()` for the “most support destroyed” case. Verification: `npm.cmd run build` passes; mobile screenshot `output/mobile-wheel-controls.png` shows wheel/pedal controls; real pointer hold on forward moved bulldozer from `z: 18` to `z: 13.29` with speed `8.15`; bulge test reports inward/outward `-1.08/1.08`; partial support test leaves `unsupportedFixedBlocks: 0`, drops front face blocks from `80` to `20`, and creates falling debris (`dynamicDebris: 26`); screenshots `output/wall-bulge-check-strong.png` and `output/partial-support-collapse-strong.png`; standard web-game client smoke completed using `output/smoke-actions.json`.

Latest note 2026-06-16: Added mobile touch controls to the bulldozer prototype and published them to the live `Game` Pages subpath. The prototype now renders a coarse-pointer-only control overlay with drive buttons (`W/A/S/D`) plus blade, pickup, release, recenter, and reset actions; touch input feeds a dedicated `touchControlOverride` path so it shares the same vehicle/blade control logic as keyboard input without breaking the scripted test hooks. Also fixed the idle-step gate so held touch controls actually advance physics. Verification: local mobile viewport screenshot `output/mobile-controls-check.png` shows the control pad; a touch-hold forward test moves the bulldozer from `z: 18` to `z: 9.96`; public `https://arisconstantinou.github.io/Game/destruction-prototype/` now contains `mobile-controls` and the new asset hashes `index-ByGdm7qR` / `index-7KXQEnyf`.

Latest note 2026-06-16: Changed the local prototype entrypoint to redirect to the public GitHub Pages environment so the Codex environment/localhost shortcut lands on the live shared URL instead of `127.0.0.1`. `index.html` now forwards local/LAN hosts to `https://arisconstantinou.github.io/Game/destruction-prototype/` while preserving query/hash. Added a local-dev escape hatch with `?local=1`. Verification: `npm.cmd run build` passes; Browser opening `http://127.0.0.1:5174/?brick-course-fix=5` lands on the public URL, while `http://127.0.0.1:5174/?local=1` stays local.

Latest note 2026-06-16: Published the playable test environment to GitHub Pages in `ArisConstantinou/Electrical-`. The built Pages output was generated with `GITHUB_ACTIONS=true` and `GITHUB_REPOSITORY=ArisConstantinou/Electrical-` so Vite emits asset URLs under `/Electrical-/`, then pushed to the `gh-pages` branch. GitHub's `pages-build-deployment` completed successfully and the public URL is live at `https://arisconstantinou.github.io/Electrical-/`. Verification: direct HTTP checks return `200` for the page and JS asset; in-app Browser opened the live Pages URL with title `Bulldozer Destruction Prototype`; a headless load check confirms `window.render_game_to_text` exists, `bodyCount: 400`, and no console errors.

Latest note 2026-06-16: Added GitHub Pages deployment setup after clarification that the desired shareable environment is public Pages, not a localhost or LAN address. Added `vite.config.ts` with a dynamic Pages base path derived from `GITHUB_REPOSITORY`, so project Pages deploys under `/<repo>/` while local dev stays at `/`. Added `.github/workflows/deploy-pages.yml` to build with `npm ci && npm run build`, upload `dist`, and deploy through GitHub Pages Actions. Verification: `npm.cmd run build` passes locally. Current limitation: this folder is not yet a git repository and has no GitHub remote, so there is no actual Pages URL until the project is pushed to GitHub and Pages is configured to use GitHub Actions.

Latest note 2026-06-16: Changed the dev/preview environment host from localhost-only to LAN-visible after mobile/share testing feedback. `npm run dev` now starts Vite with `--host 0.0.0.0 --port 5174`, and preview uses `--host 0.0.0.0` as well. Restarted the 5174 dev server and verified both `http://127.0.0.1:5174/` and `http://192.168.1.135:5174/` return HTTP 200; opened the in-app Browser to the LAN URL so the visible environment link can be used from another device on the same network.

Latest note 2026-06-16: Cleaned up the lower intact brick courses after feedback that the middle-to-bottom facade still did not match the upper wall. World-space intact brick rows now use normal half-brick staggering instead of the previous partial row offset, and intact facade colors use a stable per-brick grid hash plus a calmer standing-wall palette so the lower rows no longer form fake diagonal/vertical color bands. Rubble keeps the higher-contrast shared palette for broken debris. Verification: `npm.cmd run build` passes; in-app Browser refreshed to `http://127.0.0.1:5174/?brick-course-fix=5` and the HUD showed `FPS 176` while the lower courses rendered as regular staggered masonry.

Latest note 2026-06-16: Browser/performance + gravity verification pass after user reported floating upper walls and asked for mobile smoothness. Fixed a support bug introduced by the lower-row facade overlay: `physicalFacadeRowVisual` overlays are now ignored by `getStaticWallVisual()` so decorative lower-row skins cannot recursively count as structural support after the real bottom blocks are destroyed. Unsupported grouped upper rows now fragment into bounded falling debris in the support cleanup path; only purely decorative lower overlays are erased. Added `prototype_broad_front_support_collapse_for_test()` for regression coverage. Verification: `npm.cmd run build` passes; broad support collapse test removes the front face (`faceBlocks.front: 0`), leaves `unsupportedStaticVisualBlocks: 0` and `unsupportedFixedBlocks: 0`, and creates bounded debris (`dynamicDebris: 21`, `settledVisualDebris: 131`). Browser plugin screenshots show desktop `FPS 180`, mobile portrait `FPS 178`, and mobile landscape `FPS 170`; headless mobile screenshots loaded without console errors but software-rendered FPS values are not representative.

Latest note 2026-06-16: Restored the prototype render scheduler toward the previous high-refresh behavior after feedback that the HUD appeared capped at 60 FPS instead of the earlier ~240 FPS. The main loop no longer schedules through `requestAnimationFrame`, which can be clamped to the host/browser display refresh; it now targets a controlled 240 Hz foreground timer while leaving Rapier physics fixed at 60 Hz. Verification: `npm.cmd run build` passes. Headless SwiftShader remains too slow for absolute FPS validation, but the code-level 60 Hz rAF cap is removed; in-app Browser opened to `http://127.0.0.1:5174/?render-240-check=1`.

Latest note 2026-06-16: Fixed the lower physical facade band after feedback that everything below the row boundary still looked messy while the upper/middle facade looked acceptable. The lower physical rows now render through continuous per-row facade overlays, matching the static upper-row treatment; the individual per-block skins are hidden while intact so their clipped edges and repeated side faces no longer create the noisy bottom band. When a lower physical row breaks, its overlay is removed and the remaining physical block skins are revealed so destruction still exposes real per-block masonry/debris. Verification: `npm.cmd run build` passes; reset screenshot `output/physical-facade-row-check/shot-0.png` shows the bottom band using clean continuous rows; impact pass `output/physical-facade-impact-check/` completes with `dynamicDebris: 24`, `cabOverlappingDynamicBlocks: 0`, and no console errors.

Latest note 2026-06-16: Addressed feedback that intact bricks looked smashed together and FPS dropped while destroying walls. Intact masonry now uses chunkier shared-grid bricks (`3.0 x 1.45`) with a single solid mortar core and one exterior brick face for facade pieces instead of double-sided dense brick instances; physical lower rows share the same per-face visual seed so adjacent blocks no longer create block-column color seams. Live debris pressure was reduced by lowering the dynamic debris budget, settling rubble sooner, limiting structural visual releases per step, and scaling independent brick breakup down under debris pressure. Also fixed a Rapier stale-body crash in the secondary impact pass by skipping moving/target bodies already removed earlier in the same destruction step. Verification: `npm.cmd run build` passes; reset debug state reports `intactBrickVisuals: 11736` instead of the previous ~63k and no active dynamic debris; long destruction client pass completes without errors with `dynamicDebris: 24`; live headless SwiftShader sample stayed flat through impact (`FPS ~11.2 reset`, `~11.3 after`) with `dynamicDebris: 28`, `wallPiecesBroken: 56`, and `cabOverlappingDynamicBlocks: 0`. Screenshots: `output/brick-reset-check/shot-0.png`, `output/brick-live-impact.png`.

Latest note 2026-06-14: Fixed the startup 3 FPS regression by cutting reset-time scene complexity instead of piling more features onto the overloaded test ground. The facade grid was reduced from 24x12/16-side blocks to 12x8/8-side blocks, only the lower three rows are always physical/destructible, and upper facade rows are grouped into 20 tagged visual masonry strips that stay supported at reset. Interior partitions were reduced from 92 to 36 physical wall pieces, default quality was reset to Low through settings v4, shadows are now High-only, antialiasing is disabled for the prototype renderer, and expensive airborne-debris support debug counts are cached. Also added an idle simulation skip so Rapier/support passes do not run every frame when nothing is moving. Verification: `npm.cmd run build` passes; direct reset RAF sample improved from ~8 FPS / 673 bodies to ~54 FPS / 169 bodies with `activeDynamicBodies: 0`; movement test still advances the bulldozer and breaks front blocks; required client output is in `output/perf-reset-fix-client-final/`. Note: the deterministic client HUD FPS can still display low values after virtual-time stepping, so use live RAF/browser behavior for performance judgement.

Latest note 2026-06-14: Fixed the long brick stripe debris created by grouped upper facade rows. Direct impacts against a grouped upper visual row now split it into 4-8 shorter debris chunks based on row length, while unsupported grouped rows from support-cascade cleanup erase instead of spawning giant planks or flooding Rapier. Grouped-row collapse bias was corrected so row-wide visuals do not receive fake sideways column bias. Verification: `npm.cmd run build` passes; focused visual-row hit now ends with `47` dynamic debris instead of `143`, `FPS 37` in the focused screenshot, and no long row strips across the ground; screenshot is `output/strip-fragment-fix2.png`; required client output is in `output/strip-fix-client/`; in-app Browser refreshed to `http://127.0.0.1:5174/?strip-fix-live=1`.

## 2026-06-14

- Added a support-propagation pass for upper/static facade walls after feedback that walls floated when their bases were destroyed and that remaining walls should lean/fall when the house loses lateral support. The prototype now treats lower wall rows as a support footprint: physical wall blocks release when their vertical support is gone, and upper visual-only facade blocks wake into dynamic debris when the block below is gone or when enough other walls have lost lower support. This approximates support-polygon / center-of-mass stability without making the whole 10x house physical at reset. Also fixed the “falling stuff stops in mid air” optimization bug by requiring dynamic debris to be on the ground or resting on another slow/static body before it can be converted to visual-only settled rubble. Verification: `npm.cmd run build` passes; focused support test in `output/structural-support-focused/` reduced static visual blocks from `392` to `263`, recorded `112` structural releases, and kept active debris bounded near the budget at `157`; focused airborne-settle guard kept a high slow debris body dynamic with `settledVisuals: 0`; required client output is in `output/structural-support-client/` with clean reset state (`structuralWallReleases: 0`, `airborneDynamicDebris: 0`, `bodyCount: 673`).

- Fixed roof/rubble pieces visually poking through upper static side/back house walls. Static facade wall meshes now carry face/row/column/size metadata and participate in a cheap secondary impact pass: when a high-force roof/rubble body overlaps one, that visual block is removed and replaced by a real dynamic brick debris body, while the striking debris loses momentum. Each moving piece is capped to a small number of visual-wall wakeups so one falling roof cannot awaken the whole facade. Debug output now reports `visualWallImpacts` and tagged static visual blocks. Verification: `npm.cmd run build` passes; focused roof-into-visual-wall check in `output/visual-wall-impact-focused/` changed static visual blocks from `392` to `390`, visual wall impacts from `0` to `2`, and dynamic debris from `1` to `6`; required client output is in `output/visual-wall-impact-client/`. The in-app Browser backend was closed when asked to refresh, so a local Playwright live sample was used instead and confirmed reset state `ready: true`, `visualWallImpacts: 0`, `staticVisualBlocks: 392`, `taggedStaticVisualBlocks: 392`, `bodyCount: 673`.

- Added secondary wall-on-wall destruction so moving wall/rubble pieces can damage other breakable walls only when impact force is high enough. The new pass estimates force from moving-body speed and mass, checks overlap against fixed breakable walls, applies scaled damage/impulse, and throttles repeat hits. Weak contacts now do nothing, while heavy impacts can breach/cascade wall blocks. Verification: `npm.cmd run build` passes; focused force check shows low-force impact `0` secondary impacts / `0` broken blocks, high-force impact `2` secondary impacts / `24` broken blocks / `24` dynamic debris; required client output is in `output/secondary-wall-impact-client/`; in-app Browser was opened to `http://127.0.0.1:5174/` and live HUD showed `FPS 143`.

- Converted interior room dividers into actual destructible masonry walls after feedback that they must be real walls. Each divider is now segmented into tall brick wall blocks with colliders and breakability instead of one low placeholder slab. Settled rubble also now stores collider metadata and reactivates into dynamic physics when the bulldozer approaches, fixing the ghosting caused by the prior visual-only rubble optimization. Verification: `npm.cmd run build` passes; focused output in `output/room-walls-reactivate-focused.png` shows `initialRoomPartitions 92`; reactivation test changes dynamic rubble from `35` to `53` while settled visuals drop from `172` to `154`; required client output is in `output/room-walls-reactivate-client/`; in-app Browser was opened to `http://127.0.0.1:5174/` and live HUD showed `FPS 136`.

- Replaced the hollow/paper-like broken wall visual after feedback that pieces shared the same base shape and looked like empty sheets. The previous irregular prism shell generator was removed; broken masonry visuals now use seeded clusters of solid cuboid chunks with four different layout patterns, varied chunk sizes, offsets, and slight rotations. This keeps the stable box collider but makes the visible rubble look like thick masonry instead of a cutout plane. Verification: `npm.cmd run build` passes; focused output `output/chunky-wall-rubble.png` reports `bodyCount 311`, `wallPiecesBroken 56`, `dynamicDebris 16`, `unsupportedFixedBlocks 0`; required client output is in `output/chunky-wall-client/`; in-app Browser refreshed to `http://127.0.0.1:5174/?chunky-wall-fixed=1`.

- Reworked broken brick visuals after feedback that rubble pieces looked cloned and had ugly line artifacts. Removed the drawn black crack-line overlay, replaced chipped wall visuals with seeded irregular prism geometry, used solid dusty cut-face material on broken edges instead of stretching the brick texture around every side, added a few varied small masonry chip debris on break/shatter, and increased the physical wall grid from `12x8/8 side` to `20x14/14 side` with 4 physical lower rows so broken wall blocks are smaller from the start. Avoided direct body deletion/recreation during impact because Rapier threw an unsafe aliasing error in deterministic tests; the stable path keeps the converted body and improves visual fragmentation. Verification: `npm.cmd run build` passes; focused screenshot is `output/unique-rubble-focused-4.png` with `bodyCount 311`, `wallPiecesBroken 56`, `dynamicDebris 16`, `unsupportedFixedBlocks 0`; required client output is in `output/unique-rubble-client/`; in-app Browser refreshed to `http://127.0.0.1:5174/?unique-rubble-fixed=1`.

- Fixed the false house-collapse path where pushing the main door could make the building look like it lost all support. Door panels and low wall debris can no longer trigger upper grouped facade damage, grouped upper facade strips only release after broad lower-wall undermining, and roof collapse now also requires broad support loss on that face. The tuning label now says `bridge collapse supports` so it is clear that slider controls bridge columns, not house walls. Verification: `npm.cmd run build` passes; hard doorway regression in `output/door-final-test-2.png` reports `staticVisualBlocks 20`, `visualWallImpacts 0`, and `structuralWallReleases 0` after sustained doorway driving; required client output is in `output/door-collapse-client/`.

- Cleaned up the mystery grey interior geometry after feedback asking what the circled objects were. Those were placeholder room-floor slabs and grey interior partition boxes; the large floor slabs were removed, and interior partitions now use brick material so exposed rooms read as masonry instead of random grey barriers. Verification: `npm.cmd run build` passes; focused output in `output/interior-cleanup-focused.png` reports `roomFloors 0`, `roomPartitions 4`, `brokenBlocks 48`; required client output is in `output/interior-cleanup-client/`; in-app Browser was opened to `http://127.0.0.1:5174/` and live HUD showed `FPS 156`.

- Fixed the severe FPS drop during falling rubble by adding a debris lifecycle budget. Dynamic wall/roof debris now records creation age, upper-wall shatter is skipped once shard pressure is high, roof collapse creates fewer pieces, and old/settled or over-budget rubble converts to visual-only debris so Rapier stops simulating every brick while the pile remains visible. Verification: `npm.cmd run build` passes; focused heavy destruction run in `output/rubble-budget-focused.png` broke 162 wall blocks, then after 8 seconds reported `117` active dynamic bodies/debris and `39` settled visual debris instead of an unbounded dynamic pile; required client output is in `output/rubble-budget-client/`; in-app Browser was opened to `http://127.0.0.1:5174/` and live reset HUD showed `FPS 163`.

- Restored real destruction for reachable side and back house walls after feedback that only the front responded. The front remains fully physical; the lower five rows of the back, left, and right walls now register through the same physical `wallBlocks` path, so ground-level bulldozer hits get colliders, damage, bulge, breakage, and unsupported release without bringing back the full 962-body slowdown. Added `prototype_back_damage_for_test()` alongside the existing side hook. Verification: `npm.cmd run build` passes; focused Playwright check shows startup face blocks `front 288`, `back 120`, `left 80`, `right 80`, `roof 2`, `staticVisualBlocks 392`, `totalBlocks 570`; side hit breaks 10 dynamic wall pieces and back hit breaks 4; required client output is in `output/side-back-band-client/`; in-app Browser was opened to `http://127.0.0.1:5174/` and live HUD showed `FPS 138`.

- Balanced the enlarged house facade after feedback that doors were smaller than windows and the wall had too many windows. Door trim now uses a taller/wider cap than windows, windows are smaller, and front/back/side window placement was reduced to two sparse bands with wider spacing instead of filling every row/column. Verification: `npm.cmd run build` passes; focused output in `output/facade-balance-check/` shows 12 decorated physical front blocks instead of 302; required client output is in `output/facade-balance-client/`; in-app Browser was opened to `http://127.0.0.1:5174/` with the canvas active.

- Corrected the wall realism/perf pass after feedback that swelling was not visible and some walls/roof pieces were falling as giant sheets. The local wall bulge limit was raised to `2.4`, impact bulge falloff is wider, and bulge strength now scales visibly for the 10x house. Large staged front-wall slab chunks are disabled and their helper code was removed, so front impacts stay as local block deformation/fracture instead of whole temporary wall panels. Roof collapse now fragments each roof half into 8x4 dynamic pieces instead of converting one huge roof slab to a dynamic body. Broken wall blocks detach farther along the actual impact direction. Verification: `npm.cmd run build` passes; focused output in `output/no-giant-wall-panels-check/` shows inward/outward bulge `±0.247`, impact `maxBulge: 1.142`, `activeChunks: 0`, and no unsupported fixed blocks; required client output is in `output/no-giant-panels-client/`; live Browser reload showed `FPS 94`.
- Captured a Browser/perf pass for the enlarged house slowdown. Network was not the bottleneck: desktop/mobile each loaded 7 local Vite resources with no failed responses and ~4.65 MB transfer in dev mode. The real bottleneck was runtime environment cost: the 10x house had `981` reset physics bodies, `962` wall/roof blocks, and `302` decorated wall blocks. Optimized the environment by making the visible front facade fully breakable/physical while converting the back and side facade grids to static visual-only masonry that is cleaned up on reset. Reset body count is now `309` with `288` breakable front wall blocks, 2 roof blocks, and 672 static visual facade blocks. Raised default speed tuning via settings v3: engine torque `140`, destruction speed `2.8`, gravity `-14.5`, so old saved slow settings do not silently persist. Verification: `npm.cmd run build` passes; Browser live HUD after reload shows `FPS 155`; desktop/mobile perf output is in `output/perf-front-physical-final/`; required web-game client output is in `output/perf-front-client/`. Note: the deterministic client HUD FPS can read artificially low after large virtual-time bursts, so use Browser/live RAF output for perf.
- Localized wall impact deformation/destruction so one pushed block no longer pulls the broad front wall: bulge falloff is tighter and staged front-wall chunks now select only a local rectangle around the struck block instead of the whole upper facade. Added high/falling masonry shatter behavior: low ground-level pieces stay mostly intact, while upper chunk fragments and dropped high wall panels split into smaller shard debris. Added a `destruction speed` slider to the hidden tuning panel (`H`, then Apply Settings), defaulting to `1.6`, and wired it into chunk impulse, fragment timing, and break impulses. Verification: `npm.cmd run build` passes; focused output in `output/localized-wall-shatter-focused/` shows a local impact area, `chunkSourceBricks: 14`, and `shardDebris: 28` after shatter; required client output is in `output/localized-wall-shatter-client/`; slider DOM check confirms the control exists and toggles visible.
- Doubled the bulldozer scale by applying a `bulldozerScale = 2` to chassis, cab, blade, tracks, blade mount distance, carry height, ground height, blade offsets, and matching Rapier colliders. Also doubled the door/window visual caps and allowed them to span more of each wall panel so openings read properly against the 10x house. Verification: `npm.cmd run build` passes; focused screenshots are in `output/double-dozer-openings-focused/`; required client output is in `output/double-dozer-openings-client/` with doubled dozer Y `1.64`, no cab overlap, and no unsupported fixed wall blocks after impact.
- Scaled the house target to 10x width, height, and depth while increasing the structural panel grid to 24x12 front/back and 16x12 sides, expanding ground/world/camera/fog/shadow ranges, and moving the bridge test behind the larger house. Door/window trim now uses larger world-size dimensions and appears across every usable floor row, so openings no longer look tiny on the enlarged facade. Verification: `npm.cmd run build` passes; required client output is in `output/house-x10-client-final/`; focused facade/angle screenshots are in `output/house-x10-focused2/`, with reset state showing 962 wall blocks and 302 decorated blocks.
- Remade the brick wall UV/material path so wall blocks, the two staged front-wall slab chunks, and their later fragment bricks each get generated brick materials with texture repeat based on their actual world dimensions instead of stretching one shared image. Generated brick textures/materials are disposed when their mesh is removed. Verification: `npm.cmd run build` passes; required client output is in `output/wall-uv-client/`; focused output in `output/wall-uv-focused/` captures the initial facade and the exact active-chunk stage with 2 chunks / 48 source bricks showing repeated brick courses instead of enlarged texture panels.
- Enlarged the house target into a wider/deeper two-floor shell, fixed the sloped roof orientation, scaled doors/windows up, and added simple interior room floor zones plus partition walls for living/kitchen/back-room sections. Verification: `npm.cmd run build` passes; focused output in `output/bigger-house-focused/` confirms 230 house blocks, 40 decorated exterior blocks, 4 room floors, and 4 interior partitions at reset; required client output is in `output/bigger-house-client/` with no cab overlap and no unsupported fixed wall blocks after impact.

## 2026-06-13

- Started `destruction-prototype` as a separate Vite/Three/Rapier/lil-gui project instead of modifying TankRammer. Latest npm package versions checked before scaffolding: `three@0.184.0`, `vite@8.0.16`, `@dimforge/rapier3d-compat@0.19.3`, and `lil-gui@0.21.0`.
- Implemented the Phase 1 greybox slice in `src/main.ts`: fixed-timestep Rapier world, kinematic bulldozer/blade controller for reliable movement, physical debris after breakage, four-row destructible wall chunks, fictional bridge deck with eight two-stage supports, threshold-based deck drop, assisted G/X debris pickup/release with carry mass limit, follow/orbit camera, lil-gui tuning, HUD, and `render_game_to_text` / `advanceTime` test hooks.
- Verification: `npm.cmd run build` passes. Browser deterministic checks pass for drive, wall breakage, pickup/release, four destroyed supports, and deck collapse. Desktop screenshot output is in `output/prototype-client-final/`. Mobile portrait and landscape screenshots plus canvas pixel/movement checks are in `output/mobile-checks-final/`.
- Known caveats: Rapier compat makes the JS bundle large in this prototype; Vite reports a chunk-size warning. Rapier also emits a deprecated-init warning through the compat package. WebGL `ReadPixels` warnings in mobile logs come from the test harness pixel sampling, not gameplay.
- Added always-on mouse camera controls: left drag rotates, mouse wheel zooms, right/middle drag or shift-left-drag pans, and `C` recenters behind the bulldozer. The debug tuning panel is hidden by default and can be toggled with `H`. Verification: `npm.cmd run build` passes; web-game client screenshot is in `output/camera-client-final2/`; targeted mouse test proves yaw, pitch, zoom distance, and pan offset all change, with screenshot/results in `output/camera-mouse-check-final3/`.
- Upgraded the test-ground destruction pass for the user's five physics issues: wall cells now fracture into smaller dynamic rubble instead of one large tetris block, unsupported upper wall rows fall after lower support is removed, chassis front/rear/sides can damage breakables, dozer speed/acceleration while pushing was increased, and loose rubble is separated away from bulldozer/blade volumes to reduce visual merging. Verification: `npm.cmd run build` passes; deterministic physics checks in `output/physics-upgrade-check/` pass for faster pushing, fragment creation, support collapse, reverse damage, side damage, and bridge collapse; the standard web-game client output is in `output/physics-client/`; pickup/release regression output is in `output/pickup-regression/`.
- Current realism limit: wall damage is still grid/chunk fracture, not true CSG/mesh cutting. It now approximates blade/body-shaped holes through impact probes and fragments, which is a practical prototype step before investing in real destructible mesh or voxel wall architecture.
- Fixed input confusion: Space is now swallowed by the page but does not brake, reset, or activate the hidden tuning UI. The lil-gui reset button was removed; reset is only `T`. Canvas is focusable and focuses on click, and keydown/keyup handling runs in capture phase with normalized WASD/arrow mappings. Verification: `npm.cmd run build` passes; `output/input-fix-check/` confirms Space does not move/reset, `W` moves, and ArrowUp moves after clicking the canvas; standard client output is in `output/input-client/`.
- Fixed settings reliability: lil-gui controls now edit draft settings only, and an explicit `Apply Settings` button copies them into live physics/rendering, saves them to localStorage, applies quality, and refocuses the canvas. Applied settings persist through browser refresh. Camera follow interpolation was removed and HUD backdrop blur was disabled to avoid smeared/distorted motion while driving. Verification: `npm.cmd run build` passes; `output/settings-apply-check/` confirms changing quality does not apply immediately, movement still works before/after Apply, quality persists after refresh, and movement still works after refresh; standard client output is in `output/settings-client/`.
- Replaced the tetris/chunk wall prototype with one solid deformable wall mesh and one fixed wall collider. Wall impacts now dent the mesh locally instead of spawning small block rubble: forward blade/body hits push the surface inward, reverse/from-inside hits push it outward, and side contacts infer push direction from the contact side. Verification: `npm.cmd run build` passes; `output/solid-wall-check/result.json` confirms wall dents/breaches change while physics body count stays at 15; standard web-game client output is in `output/solid-client/`.
- Current realism limit: this is still mesh deformation plus a simple solid collider, not true masonry material simulation, CSG cutting, or finite-element destruction. It is intentionally replacing the fake box pile first so the next iteration can focus on believable dent shape, breach visuals, and inside/outside collision behavior.
- Superseded the flexible wall deformation pass after user feedback: the brick wall mesh is now a flat rigid slab again, and impacts add large rigid pushed wall-slab panels/opening marks as children of that same wall object instead of moving vertices. Repeated hits reuse nearby panels, so the wall no longer grows liquid/fabric bulges and does not spawn tetris rubble bodies. Heavy accumulated wall damage converts the whole wall entity to one dynamic rigid body so it can break/fall as a solid unit. Verification: `npm.cmd run build` passes; required web-game client output is in `output/rigid-wall-client/`; focused wall probe `output/rigid-wall-focused/result.json` confirms body count stays at 15, slab impacts are capped, and the screenshot `wall-slab-impact.png` shows rigid brick impact zones.
- Changed the prototype bridge road deck from four adjacent physics slabs into one continuous fixed deck entity with lane-marking children only, removing visible road-block seams and reducing startup body count from 15 to 12. Bridge column height now derives from the deck underside so support tops meet the slab instead of stopping short. Verification: `npm.cmd run build` passes; required web-game client output is in `output/single-bridge-deck-client/`; focused screenshot/checks are in `output/single-bridge-focused/`; bridge destruction link still marks one dropped deck after threshold in `output/single-bridge-collapse/`.
- Corrected the wall damage prototype after feedback that movable wall slabs and black opening boxes looked unrealistic. The brick wall now remains a fixed physics body regardless of accumulated damage, and impact feedback uses flat surface scars/crack decals only; no protruding mini-wall slab is spawned and no black rectangle/opening material is used. Verification: `npm.cmd run build` passes; required web-game client output is in `output/fixed-wall-client/`; focused wall check in `output/fixed-wall-focused/` confirms body count stays at 12, active dynamic bodies do not increase, and wall damage marks appear without moving the wall.
- Restored wall physics to the start-of-prototype behavior after user requested the original test-environment feel back. The wall is again built from 8x4 fixed breakable brick blocks; impacted blocks convert to dynamic physics bodies and can be pushed/carried like the early greybox test. The later solid-wall scar/decal system and unused black/decal materials were removed from the active wall path. Verification: `npm.cmd run build` passes; required web-game client output is in `output/wall-blocks-client/`; focused check in `output/wall-blocks-focused/` confirms startup body count is 43, breaking 8 wall blocks creates 8 dynamic bodies, and the bridge remains on the one-piece deck setup.
- Fixed four wall-block issues from the screenshot feedback: wall blocks no longer shrink away from each other, brick texture repeat was reduced so bricks read larger, the bulldozer cab/top now participates in the damage probe instead of passing through wall blocks visually, and unsupported wall blocks above broken supports are released as dynamic bodies with a tipping impulse. Verification: `npm.cmd run build` passes; required client output is in `output/wall-four-fixes-client-final/`; focused checks are in `output/wall-four-fixes-focused2/` and confirm 32 wall blocks, zero initial unsupported blocks, bottom support loss cascades through all rows, drive impact breaks upper rows, and `unsupportedFixedBlocks` remains 0.
- Fixed remaining cab/head brick penetration: the visible cab now has a matching Rapier collider attached to the bulldozer kinematic body, dynamic debris separation also checks the cab volume, and `render_game_to_text` reports `wall.cabOverlappingDynamicBlocks` for regression testing. Verification: `npm.cmd run build` passes; focused output is in `output/cab-collider-focused/` and standard client output is in `output/cab-collider-client-final/`, both showing `cabOverlappingDynamicBlocks: 0` after wall impact.
- Fixed lingering hanging top-span wall blocks: `releaseUnsupportedWallBlocks()` now runs every simulation step, and unsupported blocks are detached out of the wall plane with stronger tipping impulse when converted to dynamic bodies. Added `prototype_undermine_wall_span_for_test()` to reproduce the exact lower-support-removed/top-lintel case. Verification: `npm.cmd run build` passes; focused output is in `output/top-span-collapse-focused2/` showing rows `[4,4,4,4]`, `unsupportedFixedBlocks: 0`, and visibly detached center span; required client output is in `output/top-span-client-final/` with `unsupportedFixedBlocks: 0`.
- Replaced the single flat wall target with a small brick house test target: front/back/left/right breakable wall block grids plus two sloped roof physics slabs. The house was moved closer to the bulldozer spawn (`front z=7`, `back z=1.8`) so it is clearly separated from the bridge test at `z=-18`. Wall impacts now apply a small rigid per-block bulge in the incoming impulse direction before breakage, so driving in pushes the wall inward and reversing/exiting pushes it outward without returning to the liquid/flexible wall mesh. Verification: `npm.cmd run build` passes; focused checks in `output/house-physics-focused-final/` confirm 98 house blocks, 2 roof blocks, no unsupported fixed wall blocks at reset, inward bulge `-0.054`, outward bulge `0.054`; required client output is in `output/house-physics-client-final/`. Known warnings remain the Rapier deprecated init message and WebGL ReadPixels warnings from screenshot capture.
- Upgraded the house target into a two-floor version by increasing the breakable wall grid to 6 rows and adding attached door/window trim to wall blocks. The trim is child geometry on the same breakable blocks, so it moves/falls with the brick pieces instead of floating. Added blade-top rest damping for dynamic debris so bricks sitting on the shovel are settled and damped rather than being side-separated every frame, reducing bounce/flicker while preserving pushing collision. Verification: `npm.cmd run build` passes; focused output in `output/two-floor-house-focused/` confirms 146 house blocks, 26 decorated blocks, 6 front rows, inward/outward bulge still works, and after-drive cab overlap remains 0 with blocks resting on the blade; required client output is in `output/two-floor-house-client/`.
- Tuned wall impact away from an explosive feel. Wall blocks now convert to dynamic bodies with stronger damping, lower restitution, reduced direct impulse, lower angular velocity, smaller detach offsets, and unsupported rows release one layer at a time instead of cascading through the whole wall instantly. Vehicle damage probes use softer wall-specific impulses while preserving stronger column/bridge impacts. Verification: `npm.cmd run build` passes; focused output in `output/heavy-wall-impact-focused/` shows the opening slumping/piling instead of blasting outward with `unsupportedFixedBlocks: 0` and `cabOverlappingDynamicBlocks: 0`; required client output is in `output/heavy-wall-impact-client/`.
- Added staged front-wall masonry failure to better match the user’s reference: low entry blocks still push inward as brick-sized pieces, but the upper front wall now temporarily splits into two heavy slab chunks with a visible central crack/gap. Those chunks then fragment back into brick-sized dynamic debris after time/further pushing. This is implemented only for the front entry wall for now; side/back walls keep the simpler block behavior. Verification: `npm.cmd run build` passes; focused output in `output/staged-wall-failure-focused2/` confirms the mid-stage has 2 active chunks carrying 32 source bricks and the late-stage has 0 chunks with increased brick debris; required client output is in `output/staged-wall-failure-client/`.
- Added `R` FPS camera mode after user clarified they wanted first-person view, not orbit follow. The camera now mounts on top of the bulldozer cab/head, uses mouse-look with pointer lock, and arrow keys rotate the camera look direction while FPS mode is active. WASD remains vehicle movement in FPS mode, and A/D now pivots the bulldozer continuously even from a standstill; outside FPS mode arrow keys still drive as before. Debug payload now reports camera mode/FPS look offsets and bulldozer yaw for regression testing. Verification: `npm.cmd run build` passes; focused Playwright checks show `R` changes camera mode to `fps`, ArrowRight/ArrowUp alter `fpsYawOffset`/`fpsPitch`, W+D still moves the bulldozer by ~9.7 units, and stationary A/D changes yaw (`0 -> 2.041 -> 0.077`); screenshot `output/fps-camera-cab-roof-test.png`; required client output is in `output/fps-camera-client/`; in-app Browser opened to `http://127.0.0.1:5174/?fps-camera-fixed=1`.
- Fixed reversed steering while backing up. Bulldozer steering no longer multiplies by `Math.sign(dozerSpeed)`, so `A` and `D` keep the same tank-style yaw direction whether moving forward, backward, or standing still. Verification: `npm.cmd run build` passes; focused Playwright check shows reverse+A and forward+A both produce positive yaw, reverse+D and forward+D both produce negative yaw; required client output is in `output/reverse-steering-client/`; in-app Browser opened to `http://127.0.0.1:5174/?reverse-steering-fixed=1`.
- Stabilized the gravity/performance regression after feedback that FPS dropped while driving, upper walls floated again, and broken masonry looked like weird repeated shelves. The per-frame structural scan now only runs secondary impact/chunk/fracture passes when dynamic structural debris or active wall chunks actually exist, grouped upper facade support no longer lets visual rows support each other forever, and grouped visual row impacts produce only a few compact debris blocks instead of long plank-like strips. Chipped wall visuals were simplified back to a single solid brick block and falling wall shatter is capped at two blocks per step with a live debris pressure guard. The unsupported static-wall debug count is now cached so HUD diagnostics do not become the bottleneck. Verification: `npm.cmd run build` passes; focused checks in `output/gravity-perf-focused/` show reverse-only movement keeps `bodyCount 400`, `activeDynamicBodies 0`, `dynamicDebris 0`, and `unsupportedStaticVisualBlocks 0`; support cleanup reports `unsupportedStaticVisualBlocks 0`; required client output is in `output/gravity-perf-client/`; in-app Browser opened to `http://127.0.0.1:5174/?gravity-perf-cleanup=live`.
- Reworked broken masonry after feedback that the UV bricks painted on large blocks must become independent objects. Broken wall blocks now first try a bounded independent-brick physics path: the coarse panel body is removed and replaced with multiple smaller dynamic brick bodies while debris pressure is low. When pressure is high, fallback visuals are still built from individual cuboid brick meshes rather than one UV-stretched slab. Added debug counts for `independentBrickVisuals` and `independentPhysicalBricks`. Verification: `npm.cmd run build` passes; focused breakage output in `output/independent-bricks-focused/` reports `independentPhysicalBricks 9`, `independentBrickVisuals 368`, `dynamicDebris 15`, `bodyCount 405`, and `unsupportedStaticVisualBlocks 0`; required client output is in `output/independent-bricks-client/`; in-app Browser opened to `http://127.0.0.1:5174/?independent-bricks-live=1`.
- Applied the independent-brick look to the intact building facade without spawning every brick as a live physics body at reset. Intact front/back/side/static rows and interior partitions now render as staggered cuboid brick instances over a mortar bed, with chunkier brick scale matching the user's reference instead of dense UV-like noise. Broken wall blocks still convert into independent physical brick bodies/visuals on impact. Verification: `npm.cmd run build` passes; Browser live state at `http://127.0.0.1:5174/?chunky-intact-bricks-final=live` reports `bodyCount 400`, `totalBlocks 274`, `staticVisualBlocks 40`, and `intactBrickVisuals 16594`; final screenshot saved at `C:\Users\arz0r\Desktop\Drag and drop\Drag and drop\game\chunky-intact-bricks-final.png`.
- Fixed the lower-wall brick pattern after the user marked the upper wall as correct and the lower physical rows as wrong. Intact brick visuals now sample a shared world-space brick grid, so lower physical collider blocks no longer restart their own mini brick pattern and the visible courses line up like the upper static rows. Verification: `npm.cmd run build` passes; Browser screenshot saved at `C:\Users\arz0r\Desktop\Drag and drop\Drag and drop\game\shared-brick-grid-live.png`; required client run completed in `output/shared-brick-grid-client/`.
- Tuned the intact wall brick palette toward the user's circled broken-brick red/brown color. The intact brick instances now use warmer red vertex colors, stronger warm emissive fill, and darker red mortar so the full wall reads closer to the broken masonry color instead of black/dark wallpaper. Verification: `npm.cmd run build` passes; Browser screenshot saved at `C:\Users\arz0r\Desktop\Drag and drop\Drag and drop\game\wall-color-brick-red-live.png`; required client run completed in `output/wall-color-brick-red-client/`.
- Follow-up for "turn them into individual physics bricks": removed the actual wall-block fallback that converted damaged/falling masonry into fake chipped slab panels. Physical wall breaks and falling wall shatter now force replacement with independent brick-sized dynamic bodies; if conversion fails, the source wall block is removed instead of becoming a UV-stretched slab. Static/visual facade row impacts now also fragment into a bounded grid of individual brick bodies rather than a few long row segments. Verification: `npm.cmd run build` passes; Browser debug wall-break hook reports `independentPhysicalBricks: 96`, `independentBrickVisuals: 96`, `activeChunks: 0`, `chippedWallSlabs: 0`; reset remains `bodyCount: 400` with `intactBrickVisuals: 44550`, so startup does not spawn every facade brick as live physics. Static support stress now produces individual brick debris too, but heavy all-at-once collapse can still drop FPS because hundreds of brick objects are active/settled at once. Required smoke client completed in `output/individual-brick-physics-smoke/` but did not emit a screenshot for the short no-input action burst; in-app Browser screenshots were inspected instead.
- Fixed the untouched-vs-touched brick color mismatch. The intact wall instanced bricks and broken/rubble bricks now share one bright terracotta display palette, the mortar backing was warmed, and `vertexColors` was removed from the instanced brick material because it was multiplying the instance colors against missing geometry vertex colors and making untouched walls render nearly black. Verification: `npm.cmd run build` passes; in-app Browser reload shows untouched wall bricks in the same bright varied terracotta family as the broken brick area, and `prototype_break_wall_for_test(8)` reports `independentPhysicalBricks: 96`, `independentBrickVisuals: 96`, `chippedWallSlabs: 0`; required smoke client completed in `output/brick-color-match-smoke/` but only emitted the action file for the short no-input burst, so in-app Browser screenshots were used for visual inspection.
- Deep Browser check for the weird pale/non-blending wall: root cause was display RGB being fed to Three as linear RGB, which washed the shared terracotta palette into peach/pink and made the intact instanced wall disagree with the touched brick patch. `makeDisplayBrickColor()` now writes sRGB colors, the shared palette/mortar were darkened toward the broken-brick colors, and intact/broken masonry paths share `masonryBrickLength`/`masonryBrickHeight` sizing so the damaged area no longer uses a separate scale. Verification: `npm.cmd run build` passes; in-app Browser at `http://127.0.0.1:5174/?individual-brick-physics-live=1&brick-blend-check=1` reports `bodyCount: 400`, `intactBrickVisuals: 63246`, material `vertexColors: false`, and a forced break creates `independentPhysicalBricks: 80`, `independentBrickVisuals: 80`, `chippedWallSlabs: 0`; required client smoke completed in `output/brick-blend-deep-check-client/` but again only emitted the short action file, so Browser screenshots/debug payload were used for visual inspection.
